services:
  bangbangcai:
    container_name: bangbangcai
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:5173:5173"
    volumes:
      - ./data:/app/data
      - ./public/community:/app/public/community
